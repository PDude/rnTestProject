import {StyleSheet} from 'react-native';

export const useStyles = () =>
  StyleSheet.create({
    container: {
      alignSelf: 'center',
    },
    item: {
      backgroundColor: '#fcba03',
    },
  });
